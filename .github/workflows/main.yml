name: test
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!

  release:
    runs-on: ubuntu-latest
    environment:
      name: dev
      url: 'https://julioarruda.com'
    needs: build
    steps:
      - name: Run a one-line script
        run: echo Hello, world!

